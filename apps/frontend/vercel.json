{
    "buildCommand": "corepack enable && corepack prepare pnpm@9.1.0 --activate && cd ../.. && pnpm install && pnpm --filter @code-legends/api exec -- prisma generate && cd apps/frontend && pnpm build",
    "outputDirectory": ".next",
    "installCommand": "corepack enable && corepack prepare pnpm@9.1.0 --activate && cd ../.. && pnpm install",
    "framework": "nextjs"
}