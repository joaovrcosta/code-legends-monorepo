#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Executar lint em todos os workspaces
# Permitir que falhe em alguns workspaces sem bloquear o commit
# (você pode remover o || true depois de corrigir os erros)
pnpm lint || echo "Lint falhou em alguns workspaces, mas continuando..."
